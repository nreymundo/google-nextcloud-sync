# Example environment variables for g2nc
# Copy to ".env" and adjust values. Do NOT commit real secrets.

# Nextcloud
# App password generated from Nextcloud Security settings (recommended over account password)
NEXTCLOUD_APP_PASSWORD=changeme-app-password

# Google OAuth credentials
# Provide ONE of the following:
# 1) Inline JSON credentials as a single line:
# GOOGLE_CREDENTIALS_JSON={"installed":{"client_id":"...","project_id":"...","auth_uri":"https://accounts.google.com/o/oauth2/auth","token_uri":"https://oauth2.googleapis.com/token","client_secret":"...","redirect_uris":["http://localhost"]}}
# 2) A path to a JSON file mounted inside the container or available locally:
GOOGLE_CREDENTIALS_FILE=/data/google_credentials.json

# Optional: Timezone inside the container (affects logs/time-based defaults)
TZ=UTC